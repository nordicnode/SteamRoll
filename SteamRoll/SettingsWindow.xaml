<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="SteamRoll.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SteamRoll Settings"
        Height="520" Width="480"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False">

    
    <Window.Resources>
        <!-- Text Styles -->
        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#8B5CF6"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        
        <Style x:Key="SettingLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#E4E4E7"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
        
        <Style x:Key="SettingDescription" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="#71717A"/>
            <Setter Property="Margin" Value="0,5,0,0"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
        
        <!-- Button Style -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#8B5CF6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#7C3AED"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#6D28D9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#27272A"/>
            <Setter Property="Foreground" Value="#E4E4E7"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3F3F46"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- TextBox Style -->
        <Style x:Key="SettingsTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#27272A"/>
            <Setter Property="Foreground" Value="#E4E4E7"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        
        <!-- ComboBox Style -->
        <Style x:Key="SettingsComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#27272A"/>
            <Setter Property="Foreground" Value="#E4E4E7"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="Border" 
                                    Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="30"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter x:Name="ContentSite"
                                                      IsHitTestVisible="False"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      Margin="{TemplateBinding Padding}"
                                                      VerticalAlignment="Center"/>
                                    <ToggleButton x:Name="ToggleButton"
                                                  Grid.ColumnSpan="2"
                                                  Focusable="False"
                                                  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                  ClickMode="Press"
                                                  Background="Transparent"
                                                  BorderThickness="0">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Grid Background="Transparent">
                                                    <Path x:Name="Arrow"
                                                          Grid.Column="1"
                                                          HorizontalAlignment="Right"
                                                          VerticalAlignment="Center"
                                                          Margin="0,0,10,0"
                                                          Data="M 0 0 L 4 4 L 8 0 Z"
                                                          Fill="#71717A"/>
                                                </Grid>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>
                                </Grid>
                            </Border>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Border x:Name="DropDownBorder"
                                        Background="#27272A"
                                        BorderBrush="#3F3F46"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        MinWidth="{TemplateBinding ActualWidth}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                        Margin="0,2,0,0">
                                    <ScrollViewer Margin="4" SnapsToDevicePixels="True">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#8B5CF6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- ComboBoxItem Style -->
        <Style x:Key="SettingsComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#E4E4E7"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3F3F46"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#8B5CF6"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        
        <!-- CheckBox Style -->
        <Style x:Key="SettingsCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#E4E4E7"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,5"/>
        </Style>
    </Window.Resources>
    <!-- Window Container with rounded corners -->
    <Border Background="#1C1C1E" CornerRadius="8" BorderBrush="#3F3F46" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Custom Title Bar -->
            <Border Grid.Row="0" Background="#18181B" CornerRadius="8,8,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0">
                        <TextBlock Text="⚙️" FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="Settings" Foreground="#E4E4E7" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button x:Name="CloseButton" 
                            Content="✕" 
                            HorizontalAlignment="Right" 
                            VerticalAlignment="Center"
                            Margin="0,0,8,0"
                            Width="28" Height="28"
                            Click="CloseButton_Click"
                            Background="Transparent"
                            Foreground="#71717A"
                            BorderThickness="0"
                            FontSize="14"
                            Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#EF4444"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>
            
            <!-- Main Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="24">
                <StackPanel>
                    <!-- Output Settings -->
                    <TextBlock Text="📂 Output Settings" Style="{DynamicResource SectionHeader}"/>
                    <Border Background="#27272A" CornerRadius="8" Padding="16" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="Package Output Folder" Style="{DynamicResource SettingLabel}"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="OutputPathBox" IsReadOnly="True" Margin="0,0,8,0" Style="{DynamicResource SettingsTextBoxStyle}"/>
                                <Button Grid.Column="1" Content="Browse..." Click="BrowseOutput_Click" Style="{DynamicResource SecondaryButton}"/>
                            </Grid>
                            <TextBlock Text="This is where packaged games will be saved." Style="{DynamicResource SettingDescription}"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Package Settings -->
                    <TextBlock Text="📦 Package Settings" Style="{DynamicResource SectionHeader}"/>
                    <Border Background="#27272A" CornerRadius="8" Padding="16" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="Default Emulator Mode" Style="{DynamicResource SettingLabel}"/>
                            <ComboBox x:Name="PackageModeCombo" Width="200" HorizontalAlignment="Left" Style="{DynamicResource SettingsComboBoxStyle}">
                                <ComboBoxItem Content="Goldberg Emulator" Tag="Goldberg" Style="{DynamicResource SettingsComboBoxItemStyle}"/>
                                <ComboBoxItem Content="CreamAPI" Tag="CreamApi" Style="{DynamicResource SettingsComboBoxItemStyle}"/>
                            </ComboBox>
                            <TextBlock Text="Goldberg is recommended for most games. CreamAPI is useful when Goldberg doesn't work." Style="{DynamicResource SettingDescription}"/>
                            
                            <CheckBox x:Name="AutoAnalyzeCheck" Content="Automatically analyze games on scan" Margin="0,15,0,0" Style="{DynamicResource SettingsCheckBoxStyle}"/>

                            <TextBlock Text="Goldberg Version" Style="{DynamicResource SettingLabel}" Margin="0,15,0,5"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ComboBox x:Name="GoldbergVersionCombo" Margin="0,0,8,0" Style="{DynamicResource SettingsComboBoxStyle}"/>
                                <Button Grid.Column="1" x:Name="DownloadGoldbergBtn" Content="⬇️ Download" Click="DownloadGoldberg_Click" Style="{DynamicResource SecondaryButton}"/>
                            </Grid>
                            <TextBlock x:Name="CurrentGoldbergVersionText" Text="Current: Unknown" Style="{DynamicResource SettingDescription}"/>
                        </StackPanel>
                    </Border>

                    <!-- Network Settings -->
                    <TextBlock Text="🌐 Network Settings" Style="{DynamicResource SectionHeader}"/>
                    <Border Background="#27272A" CornerRadius="8" Padding="16" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="Transfer Speed Limit" Style="{DynamicResource SettingLabel}"/>
                            <ComboBox x:Name="BandwidthLimitCombo" Width="200" HorizontalAlignment="Left" Style="{DynamicResource SettingsComboBoxStyle}">
                                <ComboBoxItem Content="Unlimited" Tag="0" Style="{DynamicResource SettingsComboBoxItemStyle}"/>
                                <ComboBoxItem Content="50 MB/s" Tag="52428800" Style="{DynamicResource SettingsComboBoxItemStyle}"/>
                                <ComboBoxItem Content="20 MB/s" Tag="20971520" Style="{DynamicResource SettingsComboBoxItemStyle}"/>
                                <ComboBoxItem Content="10 MB/s" Tag="10485760" Style="{DynamicResource SettingsComboBoxItemStyle}"/>
                                <ComboBoxItem Content="5 MB/s" Tag="5242880" Style="{DynamicResource SettingsComboBoxItemStyle}"/>
                                <ComboBoxItem Content="1 MB/s" Tag="1048576" Style="{DynamicResource SettingsComboBoxItemStyle}"/>
                            </ComboBox>
                            <TextBlock Text="Limit bandwidth usage during game transfers." Style="{DynamicResource SettingDescription}"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Notifications -->
                    <TextBlock Text="🔔 Notifications" Style="{DynamicResource SectionHeader}"/>
                    <Border Background="#27272A" CornerRadius="8" Padding="16" Margin="0,0,0,20">
                        <StackPanel>
                            <CheckBox x:Name="ToastNotificationsCheck" Content="Show toast notifications" Style="{DynamicResource SettingsCheckBoxStyle}"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Cache & Data -->
                    <TextBlock Text="🗑️ Cache &amp; Data" Style="{DynamicResource SectionHeader}"/>
                    <Border Background="#27272A" CornerRadius="8" Padding="16" Margin="0,0,0,20">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <Button x:Name="ClearCacheButton" Content="Clear Game Cache" Click="ClearCache_Click" Style="{DynamicResource SecondaryButton}" Margin="0,0,10,0"/>
                                <TextBlock x:Name="CacheStatsText" VerticalAlignment="Center" Foreground="#71717A" FontSize="12"/>
                            </StackPanel>
                            <TextBlock Text="Clears cached game information. Games will be re-scanned on next refresh." Style="{DynamicResource SettingDescription}"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Logs -->
                    <TextBlock Text="📋 Logs" Style="{DynamicResource SectionHeader}"/>
                    <Border Background="#27272A" CornerRadius="8" Padding="16" Margin="0,0,0,20">
                        <StackPanel>
                            <Button Content="📋 View Application Logs" Click="ViewLogs_Click" Style="{DynamicResource SecondaryButton}" HorizontalAlignment="Left"/>
                            <TextBlock Text="View recent application logs for troubleshooting." Style="{DynamicResource SettingDescription}"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Backup & Restore -->
                    <TextBlock Text="💾 Backup &amp; Restore" Style="{DynamicResource SectionHeader}"/>
                    <Border Background="#27272A" CornerRadius="8" Padding="16" Margin="0,0,0,20">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="📤 Export Settings" Click="ExportSettings_Click" Style="{DynamicResource SecondaryButton}" Margin="0,0,10,0"/>
                                <Button Content="📥 Import Settings" Click="ImportSettings_Click" Style="{DynamicResource SecondaryButton}"/>
                            </StackPanel>
                            <TextBlock Text="Backup your configuration or transfer it to another computer." Style="{DynamicResource SettingDescription}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
            
            <!-- Bottom Buttons -->
            <Border Grid.Row="2" Background="#18181B" CornerRadius="0,0,8,8" Padding="24,16">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel" Click="Cancel_Click" Style="{DynamicResource SecondaryButton}" Margin="0,0,10,0"/>
                    <Button Content="Save Changes" Click="Save_Click" Style="{DynamicResource PrimaryButton}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>


