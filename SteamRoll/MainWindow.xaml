<Window x:Class="SteamRoll.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:SteamRoll.Controls"
        mc:Ignorable="d"
        Title="SteamRoll - LAN Game Sharing" 
        Height="800" Width="1400"
        MinHeight="650" MinWidth="1000"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="8" CornerRadius="0" GlassFrameThickness="0"/>
    </WindowChrome.WindowChrome>
    
    <!-- Window Resources: Extracted styles to avoid BAML loading issues in single-file publish -->
    <Window.Resources>
        <!-- Value Converters -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <!-- Header Image Border Style with hover opacity -->
        <Style x:Key="HeaderImageBorderStyle" TargetType="Border">
            <Setter Property="Opacity" Value="0.85"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="1"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Selection Checkbox Style -->
        <Style x:Key="SelectionCheckboxStyle" TargetType="CheckBox">
            <Setter Property="Opacity" Value="0.6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Opacity" Value="1"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="1"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Compatibility Dot Style -->
        <Style x:Key="CompatDotStyle" TargetType="Ellipse">
            <Setter Property="Fill" Value="#6E7681"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsPackageable}" Value="True">
                    <Setter Property="Fill" Value="#3FB950"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding PrimaryDrm}" Value="Denuvo">
                    <Setter Property="Fill" Value="#F85149"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        
        <!-- DRM Type TextBlock Style -->
        <Style x:Key="DrmTypeTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
            <Setter Property="Text" Value="{Binding PrimaryDrm}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding PrimaryDrm}" Value="SteamStub">
                    <Setter Property="Foreground" Value="{DynamicResource SuccessBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding PrimaryDrm}" Value="Denuvo">
                    <Setter Property="Foreground" Value="{DynamicResource ErrorBrush}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        
        <!-- DLC Badge Border Style -->
        <Style x:Key="DlcBadgeBorderStyle" TargetType="Border">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#3A2A5C" Offset="0"/>
                        <GradientStop Color="#4A2A4C" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding HasDlc}" Value="True">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        
        <!-- Packaged Badge Border Style -->
        <Style x:Key="PackagedBadgeBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="#1A3A2E"/>
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsPackaged}" Value="True">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsReceivedPackage}" Value="True">
                    <Setter Property="Background" Value="#1A2A3A"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        
        <!-- Packaged Icon TextBlock Style -->
        <Style x:Key="PackagedIconTextStyle" TargetType="TextBlock">
            <Setter Property="Text" Value="✓"/>
            <Setter Property="Foreground" Value="#3FB950"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsReceivedPackage}" Value="True">
                    <Setter Property="Text" Value="📥"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        
        <!-- Packaged Label TextBlock Style -->
        <Style x:Key="PackagedLabelTextStyle" TargetType="TextBlock">
            <Setter Property="Text" Value="Packaged"/>
            <Setter Property="Foreground" Value="#3FB950"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsReceivedPackage}" Value="True">
                    <Setter Property="Text" Value="Received"/>
                    <Setter Property="Foreground" Value="#58A6FF"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        
        <!-- Update Badge Border Style -->
        <Style x:Key="UpdateBadgeBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="#3E2C1A"/>
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding UpdateAvailable}" Value="True">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        
        <!-- Ready Badge Border Style -->
        <Style x:Key="ReadyBadgeBorderStyle" TargetType="Border">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Setter Property="Background" Value="#1A3326"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsPackageable}" Value="True">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        
        <!-- Package Button Style -->
        <Style x:Key="PackageButtonStyle" TargetType="Button">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#238636" Offset="0"/>
                        <GradientStop Color="#2EA043" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <!-- Open Package mode: teal/cyan color -->
                <DataTrigger Binding="{Binding IsPackaged}" Value="True">
                    <Setter Property="Background">
                        <Setter.Value>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#0D9488" Offset="0"/>
                                <GradientStop Color="#14B8A6" Offset="1"/>
                            </LinearGradientBrush>
                        </Setter.Value>
                    </Setter>
                </DataTrigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Button Icon TextBlock Style -->
        <Style x:Key="ButtonIconTextStyle" TargetType="TextBlock">
            <Setter Property="Text" Value="📦 "/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsPackaged}" Value="True">
                    <Setter Property="Text" Value="📂 "/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        
        <!-- Button Label TextBlock Style -->
        <Style x:Key="ButtonLabelTextStyle" TargetType="TextBlock">
            <Setter Property="Text" Value="Create Package"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsPackaged}" Value="True">
                    <Setter Property="Text" Value="Open Package"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <!-- Window Container with custom chrome -->
    <Border Background="{DynamicResource BackgroundDarkBrush}" CornerRadius="8" BorderBrush="#3F3F46" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Custom Title Bar -->
            <Border Grid.Row="0" Background="#18181B" CornerRadius="8,8,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0">
                        <TextBlock Text="🎮" FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="SteamRoll - LAN Game Sharing" Foreground="#A1A1AA" FontSize="12" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,4,0">
                        <!-- Minimize Button -->
                        <Button x:Name="MinimizeButton" Content="─" Width="40" Height="32" Click="MinimizeButton_Click"
                                Background="Transparent" Foreground="#71717A" BorderThickness="0" FontSize="12" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border" Background="{TemplateBinding Background}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#27272A"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <!-- Maximize Button -->
                        <Button x:Name="MaximizeButton" Content="☐" Width="40" Height="32" Click="MaximizeButton_Click"
                                Background="Transparent" Foreground="#71717A" BorderThickness="0" FontSize="12" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border" Background="{TemplateBinding Background}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#27272A"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <!-- Close Button -->
                        <Button x:Name="CloseButtonTitle" Content="✕" Width="40" Height="32" Click="CloseButton_Click"
                                Background="Transparent" Foreground="#71717A" BorderThickness="0" FontSize="12" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="0,8,0,0">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#EF4444"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
        
            <!-- Header with Gradient Accent -->
            <Border Grid.Row="1" Background="{DynamicResource BackgroundMediumBrush}">
                <Grid>
                    <!-- Gradient accent line at top -->
                    <Border Height="3" VerticalAlignment="Top" Background="{DynamicResource HeaderGradient}"/>
                

                <Grid Margin="32,20,32,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- Logo -->
                        <Border Width="48" Height="48" CornerRadius="12" Margin="0,0,16,0">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#58A6FF" Offset="0"/>
                                    <GradientStop Color="#A371F7" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <TextBlock Text="🎮" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="SteamRoll" FontSize="26" FontWeight="Bold" 
                                       Foreground="{DynamicResource TextPrimaryBrush}"/>
                            <TextBlock Text="Share your Steam library across your household" FontSize="13"
                                       Foreground="{DynamicResource TextMutedBrush}" Margin="0,2,0,0"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- View Toggle -->
                        <Border Background="{DynamicResource BackgroundLightBrush}" CornerRadius="6" Margin="0,0,16,0">
                            <StackPanel Orientation="Horizontal">
                                <Button x:Name="LibraryTabBtn" 
                                        Style="{DynamicResource SecondaryButton}" 
                                        Click="LibraryTab_Click" Padding="12,8">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="📚" FontFamily="Segoe UI Emoji" Margin="0,0,6,0"/>
                                        <TextBlock Text="Library"/>
                                    </StackPanel>
                                </Button>
                                <Button x:Name="PackagesTabBtn"
                                        Style="{DynamicResource SecondaryButton}"
                                        Click="PackagesTab_Click" Padding="12,8">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="📦" FontFamily="Segoe UI Emoji" Margin="0,0,6,0"/>
                                        <TextBlock Text="Packages"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </Border>
                        <Button Style="{DynamicResource SecondaryButton}" 
                                Click="RefreshButton_Click" Margin="0,0,12,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⟳" FontFamily="Segoe UI Emoji" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,6,0"/>
                                <TextBlock Text="Refresh"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{DynamicResource SecondaryButton}" 
                                Click="OpenOutputButton_Click" Margin="0,0,12,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📁" FontFamily="Segoe UI Emoji" Margin="0,0,6,0"/>
                                <TextBlock Text="Open Output"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{DynamicResource SecondaryButton}"
                                Click="ImportPackageButton_Click" Margin="0,0,12,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📥" FontFamily="Segoe UI Emoji" Margin="0,0,6,0"/>
                                <TextBlock Text="Import Package"/>
                            </StackPanel>
                        </Button>
                        <ToggleButton x:Name="SelectModeToggle" 
                                      Click="SelectModeToggle_Click" Margin="0,0,12,0"
                                      Background="Transparent" Foreground="{DynamicResource TextSecondaryBrush}"
                                      BorderBrush="{DynamicResource BorderSubtleBrush}" BorderThickness="1"
                                      Padding="14,8" Cursor="Hand">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="border" Background="{TemplateBinding Background}" 
                                            CornerRadius="6" Padding="{TemplateBinding Padding}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            BorderBrush="{TemplateBinding BorderBrush}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#238636"/>
                                            <Setter TargetName="border" Property="BorderBrush" Value="#238636"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#21262D"/>
                                            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="☑" FontFamily="Segoe UI Emoji" Margin="0,0,6,0"/>
                                <TextBlock Text="Select"/>
                            </StackPanel>
                        </ToggleButton>
                        <Button Style="{DynamicResource SecondaryButton}" 
                                Click="SettingsButton_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⚙" FontFamily="Segoe UI Emoji" Margin="0,0,6,0"/>
                                <TextBlock Text="Settings"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>

                </Grid>
            </Grid>
        </Border>
        
        <!-- Stats Bar -->
        <Border Grid.Row="2" Background="{DynamicResource BackgroundDarkBrush}" 
                BorderBrush="{DynamicResource BorderSubtleBrush}" BorderThickness="0,0,0,1" Padding="32,16">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Games Count -->
                <Border Grid.Column="0" Background="{DynamicResource BackgroundLightBrush}" 
                        CornerRadius="8" Padding="16,10" Margin="0,0,16,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📚" FontSize="18" Margin="0,0,10,0" VerticalAlignment="Center"
                                   FontFamily="Segoe UI Emoji" Foreground="{DynamicResource TextSecondaryBrush}"/>
                        <StackPanel>
                            <TextBlock x:Name="TotalGamesText" Text="0" FontSize="20" FontWeight="Bold"
                                       Foreground="{DynamicResource TextPrimaryBrush}"/>
                            <TextBlock Text="Games" FontSize="11" Foreground="{DynamicResource TextMutedBrush}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
                
                <!-- Packageable Count -->
                <Border Grid.Column="1" Background="{DynamicResource BackgroundLightBrush}" 
                        CornerRadius="8" Padding="16,10" Margin="0,0,16,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="✓" FontSize="18" Foreground="{DynamicResource SuccessBrush}" 
                                   Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <StackPanel>
                            <TextBlock x:Name="PackageableText" Text="0" FontSize="20" FontWeight="Bold"
                                       Foreground="{DynamicResource SuccessBrush}"/>
                            <TextBlock Text="Packageable" FontSize="11" Foreground="{DynamicResource TextMutedBrush}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
                
                <!-- Total Size -->
                <Border Grid.Column="2" Background="{DynamicResource BackgroundLightBrush}" 
                        CornerRadius="8" Padding="16,10">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="💾" FontSize="18" Margin="0,0,10,0" VerticalAlignment="Center"
                                   FontFamily="Segoe UI Emoji" Foreground="{DynamicResource TextSecondaryBrush}"/>
                        <StackPanel>
                            <TextBlock x:Name="TotalSizeText" Text="0 GB" FontSize="20" FontWeight="Bold"
                                       Foreground="{DynamicResource TextPrimaryBrush}"/>
                            <TextBlock Text="Total Size" FontSize="11" Foreground="{DynamicResource TextMutedBrush}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
                
                <!-- Network Status (visually distinct from static stats) -->
                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                    <TextBlock x:Name="NetworkStatusText" Text="Searching for peers" FontSize="12"
                               FontStyle="Italic" FontWeight="Light"
                               Foreground="{DynamicResource TextMutedBrush}" VerticalAlignment="Center"/>
                    <TextBlock x:Name="NetworkStatusDots" Text="..." FontSize="12"
                               FontStyle="Italic" FontWeight="Light"
                               Foreground="{DynamicResource TextMutedBrush}" VerticalAlignment="Center"/>
                </StackPanel>
                
                <!-- Filter Buttons (pill-style chips) -->
                <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,16,0">
                    <ToggleButton x:Name="FilterReadyBtn" Click="FilterReady_Click" 
                                  ToolTip="Show only games ready to package"
                                  Background="#1A27272A" BorderThickness="0" Padding="12,6" Margin="0,0,6,0" Cursor="Hand">
                        <ToggleButton.Template>
                            <ControlTemplate TargetType="ToggleButton">
                                <Border x:Name="border" Background="{TemplateBinding Background}" 
                                        CornerRadius="14" Padding="{TemplateBinding Padding}"
                                        BorderBrush="#3F3F46" BorderThickness="1">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#238636"/>
                                        <Setter TargetName="border" Property="BorderBrush" Value="#238636"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#2A2A2E"/>
                                        <Setter TargetName="border" Property="BorderBrush" Value="#52525B"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </ToggleButton.Template>
                        <TextBlock Text="✓ Ready" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                    </ToggleButton>
                    
                    <ToggleButton x:Name="FilterPackagedBtn" Click="FilterPackaged_Click"
                                  ToolTip="Show only packaged games"
                                  Background="#1A27272A" BorderThickness="0" Padding="12,6" Margin="0,0,6,0" Cursor="Hand">
                        <ToggleButton.Template>
                            <ControlTemplate TargetType="ToggleButton">
                                <Border x:Name="border" Background="{TemplateBinding Background}" 
                                        CornerRadius="14" Padding="{TemplateBinding Padding}"
                                        BorderBrush="#3F3F46" BorderThickness="1">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#0D9488"/>
                                        <Setter TargetName="border" Property="BorderBrush" Value="#0D9488"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#2A2A2E"/>
                                        <Setter TargetName="border" Property="BorderBrush" Value="#52525B"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </ToggleButton.Template>
                        <TextBlock Text="📦 Packaged" FontSize="11" FontFamily="Segoe UI Emoji" Foreground="{DynamicResource TextSecondaryBrush}"/>
                    </ToggleButton>
                    
                    <ToggleButton x:Name="FilterDlcBtn" Click="FilterDlc_Click"
                                  ToolTip="Show only games with DLC"
                                  Background="#1A27272A" BorderThickness="0" Padding="12,6" Margin="0,0,6,0" Cursor="Hand">
                        <ToggleButton.Template>
                            <ControlTemplate TargetType="ToggleButton">
                                <Border x:Name="border" Background="{TemplateBinding Background}" 
                                        CornerRadius="14" Padding="{TemplateBinding Padding}"
                                        BorderBrush="#3F3F46" BorderThickness="1">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#A371F7"/>
                                        <Setter TargetName="border" Property="BorderBrush" Value="#A371F7"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#2A2A2E"/>
                                        <Setter TargetName="border" Property="BorderBrush" Value="#52525B"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </ToggleButton.Template>
                        <TextBlock Text="🎁 DLC" FontSize="11" FontFamily="Segoe UI Emoji" Foreground="{DynamicResource TextSecondaryBrush}"/>
                    </ToggleButton>
                    
                    <!-- Update Available Filter -->
                    <ToggleButton x:Name="FilterUpdateBtn" Click="FilterUpdate_Click"
                                  ToolTip="Show games with updates available (package outdated)"
                                  Background="#1A27272A" BorderThickness="0" Padding="12,6" Cursor="Hand">
                        <ToggleButton.Template>
                            <ControlTemplate TargetType="ToggleButton">
                                <Border x:Name="border" Background="{TemplateBinding Background}" 
                                        CornerRadius="14" Padding="{TemplateBinding Padding}"
                                        BorderBrush="#3F3F46" BorderThickness="1">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#D29922"/>
                                        <Setter TargetName="border" Property="BorderBrush" Value="#D29922"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#2A2A2E"/>
                                        <Setter TargetName="border" Property="BorderBrush" Value="#52525B"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </ToggleButton.Template>
                        <TextBlock Text="🔄 Updates" FontSize="11" FontFamily="Segoe UI Emoji" Foreground="{DynamicResource TextSecondaryBrush}"/>
                    </ToggleButton>
                </StackPanel>
                
                <!-- Search Box -->
                <TextBox Grid.Column="5" x:Name="SearchBox" Width="250" 
                         Style="{DynamicResource SearchBoxStyle}"
                         Text="" 
                         GotFocus="SearchBox_GotFocus"
                         LostFocus="SearchBox_LostFocus"
                         TextChanged="SearchBox_TextChanged"/>
            </Grid>
        </Border>
        
        <!-- Library View Container (game list) -->
        <Grid x:Name="LibraryView" Grid.Row="3">

            <!-- Peers list (above game list, only when peers found) -->
            <StackPanel VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,10,24,0"
                        Visibility="{Binding HasPeers, Converter={StaticResource BooleanToVisibilityConverter}}">
                <Button Click="BrowsePeerButton_Click" Background="Transparent" Foreground="#58A6FF" BorderThickness="0" Cursor="Hand">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🌐 Browse Peer Library" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        
            <!-- Empty State (shown when no games) -->
            <Border x:Name="EmptyStatePanel" Visibility="Collapsed" VerticalAlignment="Center" HorizontalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="🎮" FontSize="64" FontFamily="Segoe UI Emoji" HorizontalAlignment="Center" Opacity="0.4"/>
                    <TextBlock x:Name="EmptyStateTitle" Text="No Games Found" FontSize="20" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,16,0,8"/>
                    <TextBlock x:Name="EmptyStateMessage" Text="Try adjusting your filters or adding Steam library paths in Settings."
                               FontSize="13" Foreground="{DynamicResource TextTertiaryBrush}" 
                               HorizontalAlignment="Center" TextAlignment="Center" MaxWidth="400"/>
                    <Button x:Name="EmptyStateRefreshBtn" Click="RefreshButton_Click" Margin="0,24,0,0"
                            Background="#238636" Foreground="White" BorderThickness="0" Padding="20,10" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔄 " FontFamily="Segoe UI Emoji"/>
                            <TextBlock Text="Refresh Library"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Border>

            <!-- Game List -->
            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24,16">
                <ItemsControl x:Name="GamesList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="5"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{DynamicResource GameCard}" Margin="8" Cursor="Hand"
                                MouseLeftButtonUp="GameCard_Click" Tag="{Binding}"
                                ToolTip="{Binding DetailedTooltip}" ClipToBounds="True">
                            <!-- Context Menu for game cards -->
                            <Border.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="📦 Create Package" Click="ContextMenu_Package_Click" Tag="{Binding}">
                                        <MenuItem.Style>
                                            <Style TargetType="MenuItem">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsPackaged}" Value="True">
                                                        <Setter Property="Header" Value="📂 Open Package"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </MenuItem.Style>
                                    </MenuItem>
                                    <MenuItem Header="🗑️ Delete Package" Click="ContextMenu_DeletePackage_Click" Tag="{Binding}">
                                        <MenuItem.Style>
                                            <Style TargetType="MenuItem">
                                                <Setter Property="IsEnabled" Value="{Binding IsPackaged}"/>
                                            </Style>
                                        </MenuItem.Style>
                                    </MenuItem>
                                    <Separator/>
                                    <MenuItem Header="📤 Send to Peer" Click="ContextMenu_SendToPeer_Click" Tag="{Binding}">
                                        <MenuItem.Style>
                                            <Style TargetType="MenuItem">
                                                <Setter Property="IsEnabled" Value="{Binding IsPackaged}"/>
                                            </Style>
                                        </MenuItem.Style>
                                    </MenuItem>
                                    <MenuItem Header="🔄 Sync Saves with Peer" Click="ContextMenu_SyncSaves_Click" Tag="{Binding}"/>
                                    <MenuItem Header="⚙️ Advanced Config" Click="ContextMenu_AdvancedConfig_Click" Tag="{Binding}"
                                              ToolTip="Configure Goldberg emulator settings">
                                        <MenuItem.Style>
                                            <Style TargetType="MenuItem">
                                                <Setter Property="IsEnabled" Value="{Binding IsPackageable}"/>
                                            </Style>
                                        </MenuItem.Style>
                                    </MenuItem>
                                    <Separator/>
                                    <MenuItem Header="📁 Open Install Folder" Click="ContextMenu_OpenInstallFolder_Click" Tag="{Binding}"/>
                                    <MenuItem Header="🔍 View Details" Click="ContextMenu_ViewDetails_Click" Tag="{Binding}"/>
                                    <MenuItem Header="🛡️ Verify Integrity" Click="ContextMenu_VerifyIntegrity_Click" Tag="{Binding}">
                                        <MenuItem.Style>
                                            <Style TargetType="MenuItem">
                                                <Setter Property="IsEnabled" Value="{Binding IsPackaged}"/>
                                            </Style>
                                        </MenuItem.Style>
                                    </MenuItem>
                                    <MenuItem Header="🔧 Repair from Peer" Click="ContextMenu_RepairFromPeer_Click" Tag="{Binding}"
                                              ToolTip="Pull missing/corrupt files from a peer">
                                        <MenuItem.Style>
                                            <Style TargetType="MenuItem">
                                                <Setter Property="IsEnabled" Value="{Binding IsPackaged}"/>
                                            </Style>
                                        </MenuItem.Style>
                                    </MenuItem>
                                    <MenuItem Header="🌐 Open Steam Store" Click="ContextMenu_OpenSteamStore_Click" Tag="{Binding}"/>
                                </ContextMenu>
                            </Border.ContextMenu>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="90"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                
                                <!-- Row 0: Header Image with overlay -->
                                <Grid Grid.Row="0" x:Name="HeaderImageContainer">
                                    <!-- Placeholder icon when no image loads -->
                                    <Border Background="#21262D">
                                        <TextBlock Text="🎮" FontSize="32" 
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                                   FontFamily="Segoe UI Emoji" Foreground="#6E7681"/>
                                    </Border>
                                    
                                    <!-- Game Header Image with rounded top corners -->
                                    <Border CornerRadius="10,10,0,0" Style="{DynamicResource HeaderImageBorderStyle}">
                                        <Border.Background>
                                            <ImageBrush ImageSource="{Binding HeaderImageUrl}" Stretch="UniformToFill"/>
                                        </Border.Background>
                                    </Border>
                                    
                                    <!-- Dark gradient overlay for text readability -->
                                    <Border CornerRadius="10,10,0,0">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                <GradientStop Color="#00000000" Offset="0"/>
                                                <GradientStop Color="#AA0D1117" Offset="0.7"/>
                                                <GradientStop Color="#FF0D1117" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.Background>
                                    </Border>
                                    
                                    <!-- Selection checkbox (top left) -->
                                    <CheckBox HorizontalAlignment="Left" VerticalAlignment="Top" Margin="8,8,0,0"
                                              IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                              Click="GameSelection_Click"
                                              Tag="{Binding}"
                                              Style="{DynamicResource SelectionCheckboxStyle}"/>
                                    
                                    <!-- Compatibility indicator (top right) -->
                                    <Ellipse Width="12" Height="12" Margin="0,8,8,0" 
                                             HorizontalAlignment="Right" VerticalAlignment="Top"
                                             Style="{DynamicResource CompatDotStyle}"/>
                                </Grid>

                                
                                <!-- Row 1: Content area -->
                                <Grid Grid.Row="1" Margin="14,10,14,14">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                
                                <!-- Game Name -->
                                <TextBlock Grid.Row="0" Text="{Binding Name}" 
                                           FontSize="15" FontWeight="SemiBold"
                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                           TextTrimming="CharacterEllipsis"/>
                                
                                <!-- Row 1: DRM Type only (simplified) -->
                                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,4,0,0">
                                    <TextBlock FontSize="11" Style="{DynamicResource DrmTypeTextStyle}"/>
                                </StackPanel>
                                
                                <!-- Row 2: Size and DLC Info Badges -->
                                <WrapPanel Grid.Row="2" Margin="0,12,0,0">
                                    <!-- Size Badge -->
                                    <Border Background="#21262D" CornerRadius="4" Padding="8,4" Margin="0,0,8,4">
                                        <TextBlock Text="{Binding FormattedSize}" FontSize="11" 
                                                   Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    </Border>
                                    
                                    <!-- DLC Badge (only if HasDlc) -->
                                    <Border CornerRadius="4" Padding="8,4" Margin="0,0,8,4" Style="{DynamicResource DlcBadgeBorderStyle}">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🎁" FontSize="10" Margin="0,0,4,0" FontFamily="Segoe UI Emoji"/>
                                            <TextBlock Text="{Binding DlcSummary}" 
                                                       FontSize="11" Foreground="#A371F7"/>
                                        </StackPanel>
                                    </Border>
                                    
                                    <!-- Packaged Badge (shows different for received packages) -->
                                    <Border CornerRadius="4" Padding="8,4" Margin="0,0,8,4" Style="{DynamicResource PackagedBadgeBorderStyle}">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock FontSize="10" Margin="0,0,4,0" Style="{DynamicResource PackagedIconTextStyle}"/>
                                            <TextBlock FontSize="11" Style="{DynamicResource PackagedLabelTextStyle}"/>
                                        </StackPanel>
                                    </Border>
                                    
                                    <!-- Update Available Badge -->
                                    <Border CornerRadius="4" Padding="8,4" Margin="0,0,8,4" Style="{DynamicResource UpdateBadgeBorderStyle}">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🔄" FontSize="10" Margin="0,0,4,0" FontFamily="Segoe UI Emoji"/>
                                            <TextBlock Text="Update" FontSize="11" Foreground="#D29922"/>
                                        </StackPanel>
                                    </Border>
                                    
                                    <!-- Status Badge (simplified) -->
                                    <Border CornerRadius="4" Padding="8,4" Margin="0,0,0,4" Style="{DynamicResource ReadyBadgeBorderStyle}">
                                        <TextBlock Text="Ready" FontSize="11" Foreground="{DynamicResource SuccessBrush}"/>
                                    </Border>
                                </WrapPanel>
                                

                                
                                 <!-- Row 3: Actions -->
                                <Grid Grid.Row="3" Margin="0,14,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Package Button -->
                                    <Button Grid.Column="0"
                                            Click="PackageButton_Click"
                                            Tag="{Binding}"
                                            IsEnabled="{Binding IsPackageable}"
                                            FontWeight="SemiBold"
                                            Foreground="White"
                                            BorderThickness="0"
                                            Padding="10,10"
                                            Cursor="Hand"
                                            HorizontalAlignment="Stretch"
                                            ToolTip="{Binding FormattedSize, StringFormat='Package size: ~{0}'}"
                                            Style="{DynamicResource PackageButtonStyle}">
                                        <Button.Content>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock FontSize="13" FontFamily="Segoe UI Emoji" Style="{DynamicResource ButtonIconTextStyle}"/>
                                                <TextBlock FontSize="12" Style="{DynamicResource ButtonLabelTextStyle}"/>
                                            </StackPanel>
                                        </Button.Content>
                                    </Button>
                                    
                                    <!-- Send to Peer Button (only when packaged) -->
                                    <Button Grid.Column="1"
                                            Style="{DynamicResource SecondaryButton}"
                                            Margin="8,0,0,0" 
                                            Click="SendToPeerButton_Click"
                                            Tag="{Binding}"
                                            Padding="12,10"
                                            ToolTip="Send to Peer">
                                        <Button.Visibility>
                                            <Binding Path="IsPackaged">
                                                <Binding.Converter>
                                                    <BooleanToVisibilityConverter/>
                                                </Binding.Converter>
                                            </Binding>
                                        </Button.Visibility>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="📤" FontSize="13" FontFamily="Segoe UI Emoji" Margin="0,0,4,0"/>
                                            <TextBlock Text="Send" FontSize="12" FontWeight="SemiBold"/>
                                        </StackPanel>
                                    </Button>
                                </Grid>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            </ScrollViewer>
            
            <!-- Batch Action Bar (shows when games are selected) -->
            <Border x:Name="BatchActionBar" 
                    VerticalAlignment="Bottom" 
                    HorizontalAlignment="Center"
                    Margin="0,0,0,20"
                    Visibility="Collapsed">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.5"/>
                </Border.Effect>
                <Border Background="#27272A" CornerRadius="12" Padding="20,12" BorderBrush="#3F3F46" BorderThickness="1">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="BatchSelectionText" Text="0 games selected" 
                                   Foreground="{DynamicResource TextSecondaryBrush}" 
                                   FontSize="13" VerticalAlignment="Center" Margin="0,0,20,0"/>
                        
                        <Button x:Name="BatchPackageButton" Click="BatchPackage_Click" 
                                Cursor="Hand" Margin="0,0,10,0"
                                Background="#238636" Foreground="White" BorderThickness="0" Padding="16,8">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📦 " FontFamily="Segoe UI Emoji"/>
                                <TextBlock Text="Package Selected"/>
                            </StackPanel>
                        </Button>
                        
                        <Button x:Name="BatchClearButton" Click="BatchClear_Click" 
                                Cursor="Hand" Margin="0,0,10,0"
                                Background="#3F3F46" Foreground="#E4E4E7" BorderThickness="0" Padding="16,8">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                            <TextBlock Text="Clear Selection"/>
                        </Button>
                        
                        <Button x:Name="BatchSendBtn" Click="BatchSendToPeer_Click" 
                                Cursor="Hand" IsEnabled="False"
                                Background="#58A6FF" Foreground="White" BorderThickness="0" Padding="16,8">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter TargetName="border" Property="Opacity" Value="0.4"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📤 " FontFamily="Segoe UI Emoji"/>
                                <TextBlock Text="Send to Peer"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Border>
            </Border>
        </Grid>
        
        <!-- Game Details View (hidden by default) -->
        <controls:GameDetailsView x:Name="GameDetailsView" Grid.Row="3" 
                                   Visibility="Collapsed"/>

        

        
        <!-- Status Bar -->
        <Border Grid.Row="4" Background="{DynamicResource BackgroundMediumBrush}" CornerRadius="0,0,8,8" 
                BorderBrush="{DynamicResource BorderSubtleBrush}" BorderThickness="0,1,0,0" Padding="32,14">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock x:Name="StatusText" Grid.Column="0" 
                           Text="Ready — Select a game to view details or create a package"
                           FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"
                           VerticalAlignment="Center"/>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Goldberg Emulator + DLC Unlock" FontSize="11" 
                               Foreground="{DynamicResource TextMutedBrush}" Margin="0,0,16,0"/>
                    <TextBlock Text="v1.1.0" FontSize="12" 
                               Foreground="{DynamicResource AccentPrimaryBrush}" FontWeight="SemiBold"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Global Loading Overlay -->
        <controls:LoadingOverlay x:Name="LoadingOverlay" Grid.RowSpan="5"/>
        
        <!-- Toast Notification Container -->
        <StackPanel x:Name="ToastContainer" Grid.RowSpan="5"
                    HorizontalAlignment="Right" VerticalAlignment="Bottom"
                    Margin="0,0,16,80"/>
        </Grid>
    </Border>
</Window>

